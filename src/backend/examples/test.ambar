x : int = 42;
y : float = 3.14;
name : string = "Ambar";
flag : bool = true;
uninitialized : string;

func add(a : int, b : int) -> int {
    return a + b;
}

func greet() -> void {
    print("Hello, world!");
}

func factorial(n : int) -> int {
    if (n == 0) {
        return 1;
    } else {
        return n * factorial(n - 1);
    }
}
